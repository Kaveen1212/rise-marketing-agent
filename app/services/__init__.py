# app/services/__init__.py
# Business logic layer — sits between the API routes and the data layer.
# Routes should be thin (validate input, call service, return response).
# Services contain the actual logic.
