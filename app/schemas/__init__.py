# app/schemas/__init__.py
# Pydantic schemas used for request validation and response serialization.
# These are DIFFERENT from app/models/ — models are SQLAlchemy (database tables),
# schemas are Pydantic (what the API accepts and returns).
